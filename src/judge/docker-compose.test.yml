services:
  pytest:
    image: nextjudge-judge-test
    pull_policy: never
    privileged: true
    volumes:
      - go_cache:/var/lib/nextjudge/go-cache
      - go_mod_cache:/var/lib/nextjudge/go-mod

volumes:
  go_cache:
  go_mod_cache:
