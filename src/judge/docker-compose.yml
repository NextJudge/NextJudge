# version: '3.9'
services:
  nextjudge-judge:
    build: 
      dockerfile: ./Dockerfile.monolith
      context: .
      args:
        - USAGE=prod
    privileged: true
    environment:
      - BRIDGE_HOST=nextjudge-bridge
      - BRIDGE_PORT=3000