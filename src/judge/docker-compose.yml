# version: '3.9'
services:
  nextjudge-judge:
    build: .
    privileged: true
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - BRIDGE_HOST=nextjudge-bridge # This override is not working - bug in Docker compose?
      - BRIDGE_PORT=3000
